// Injected by vite.config.ts from package.json — the welcome screen used
// to carry a hand-written label ("Phase 3b"), which drifted three phases
// and one packaged release behind reality. A version nobody maintains
// cannot go stale.
declare const __APP_VERSION__: string;
